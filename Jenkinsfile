pipeline{
	agent any
	
	stages{
		stage('Build'){
            script {
                bat "npm install"
                bat "npm run build"
            }
		}
		stage('Deploy') {
            echo "unsaonszzz?"
		}
		
	}
}
